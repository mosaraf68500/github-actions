name: first workflows

on: [push]
jobs:
  first-workflows:
    name: first step
    runs-on: macOS
    steps:
      - name: Say hello
        run: |
          echo "hello world"
          echo "hello again"